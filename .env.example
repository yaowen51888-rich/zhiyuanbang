# Docker Compose 覆盖变量。换部署域名/数据源时改这些项后：docker compose up --build
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
ALLOWED_ORIGINS=http://localhost:3000
POSTGRES_PASSWORD=gkvr

# 全量种子数据 Release 地址（首启 docker 自动下载灌库）
# 维护者发布 Release 后替换 OWNER/REPO 与 tag；留空则用 fetch_data.py 内置默认
DATA_RELEASE_URL=https://github.com/OWNER/REPO/releases/download/data-v1/seed_full.json.gz
