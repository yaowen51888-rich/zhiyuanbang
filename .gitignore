# superpowers 临时文件（brainstorm mockups、sdd ledger）
.superpowers/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# 数据库
*.db
*.sqlite3

# Node
node_modules/
.next/

# 原项目数据 dump（大文件，不入 git）
data/source_sql/*.sql

# OpenAPI 导出（生成物）
backend/openapi.json

# 工具产物与运行时状态（不入 git）
.claude/
.playwright-mcp/
.serena/
backend/*.log
data/crawl_state.json
docs/*.zip
bash.exe.stackdump
docker-compose.override.yml

# 测试与构建产物
.pytest_cache/
*.egg-info/
data/__pycache__/
frontend/tsconfig.tsbuildinfo

# 数据库与日志（保留脚本，排除运行时产物）
*.db
*.sqlite3
*.log

# 全量种子（生成物，发 Release 不入 git；由 export_seed_full.py 产出）
backend/scripts/seed_full.json
backend/scripts/seed_full.json.gz

# 前端构建产物（已在 frontend/.gitignore，根目录再保一次）
frontend/.next/
frontend/out/
frontend/dist/
